# Goals

- Maintain token-driven, sharp-edged, workstation-focused component set
- All design values from tokens.json — no hardcoded values ever
- borderRadius always tokens.radius.default (0) — no exceptions
- Progressive disclosure: ActionMenu for >2 actions, WorkflowStepper for >3 steps
